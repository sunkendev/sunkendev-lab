INSTRUCTIONS v32
----------------
PROJECT: AI Document Library System
GOAL: Design, document, and iteratively refine a five-layer vendor-agnostic generational system for managing AI-generated work using plain text files and a prompt-controlled workflow.
BACKGROUND: This session did no artifact-content work on MASTER-PROMPT.md itself. Instead it (1) ran a rigor test of the RESUME/CHECKPOINT cycle on real sustained narrative content in a new permanent test project, signal-lost, using genuinely fresh memory-less subagents across three writing rounds plus a comprehension-only round, confirming RESUME is sufficient for plot/fact continuity but — by design, since it never reads the artifact — insufficient for verifying prose-voice continuity; (2) fully diagnosed a recurring git-signing "Unverified" warning as deliberate, documented Anthropic cloud-sandbox security design unrelated to the library; and (3) found and fixed two real gaps in the ai-library-ops skill's push/fast-forward-main sync logic — one where the agent bypassed the skill by calling the underlying scripts directly, and one where the NOTE operation's documented procedure never included a sync step at all, unlike CHECKPOINT and COMMIT. The NOTE fix went through an extra round: a first fix added full CHECKPOINT-style push+ff-main to NOTE, which was then critiqued as violating the artifact's own deliberate ceremony tiering (NOTE is meant to be the lightest, highest-frequency operation) and reverted to push-only, with the actual main-sync deferred to the next CHECKPOINT/COMMIT/session end. The revert was verified by live execution, not just by re-reading the edited text.
ARTIFACT STATE: MASTER-PROMPT.md v32 — NO CHANGE, relabelled from v31. No edits to MASTER-PROMPT.md this session; all work was on ai-library-ops SKILL.md (three deployed copies kept in sync), THREAD.md NOTEs, and the signal-lost project (separate checkpoint stream, not part of this artifact).
KEY DECISIONS:
- signal-lost rigor test completed and kept permanently in the library; findings documented in projects/signal-lost/docs/2026-06-28--rigor-test-findings--claude.md
- Git-signing "Unverified" gap is environment-specific (Claude Code cloud sandbox security design), not a library bug; recorded via NOTE, not fixed
- ai-library-ops SKILL.md now warns against bypassing the skill via direct script calls, and NOTE now pushes the session branch (but does not fast-forward main) as part of its own documented procedure
- The full push+ff-main sync stays exclusive to CHECKPOINT and COMMIT — NOTE intentionally defers the main sync, preserving the deliberate ceremony-weight difference between a lightweight note and a real checkpoint
- Both this session's SKILL.md fixes were verified by live execution (real commits, real pushes, real before/after git-hash comparisons), not by inspection of the edited text alone
OPEN QUESTIONS:
- Which OPEN item to pick up next: candidates include the flow-vs-ai temp/ cleanup, the related: bare-filename hook warning, MAP.md machine-readable block, MASTER-PROMPT.md artifact type schema, Layer 4 start, an automated test suite for checkpoint.py, or — newly surfaced this session — automated test coverage for the skill's own git-sync logic (CHECKPOINT/COMMIT/NOTE), since a real bug there (NOTE missing its sync step) shipped undetected until a user follow-up question caught it
- Whether to design the proposed optional verbatim style-anchor excerpt for context.md, addressing the prose-voice-continuity gap the signal-lost test confirmed — not yet requested, just surfaced as a candidate
EXPLICITLY RULED OUT:
- Embedding full CHECKPOINT-style push+fast-forward-main logic into NOTE — tried, then reverted after critique against the artifact's own stated design (NOTE is the deliberately lightweight, anytime operation)
- Best-effort automatic deletion of merged session branches — proxy blocks it (HTTP 403) in every environment this skill runs in
- (Plus all prior ruled-out items — see v32--context.md)
NEXT TASK: Pick the next OPEN item to work, or continue ad hoc per user direction. The newly surfaced test-coverage gap for the skill's git-sync logic is a reasonable next candidate given it just caused a real, live bug this session.
PERSONA: See persona.md
STYLE AND CONSTRAINTS: Plain language. Precise. No lists where prose works better. No suggestions that contradict the established philosophy. When proposing changes to master prompt or documents, show the specific change not a full rewrite. State plan before executing any write, script, or git operation.
